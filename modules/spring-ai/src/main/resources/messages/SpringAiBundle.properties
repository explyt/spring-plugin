explyt.spring.ai.action.prop.to.yaml=Convert Properties to YAML
explyt.spring.ai.action.yaml.to.prop=Convert YAML to Properties
explyt.spring.ai.action.dto.to.jpa=Convert Dto to Entity
explyt.spring.ai.action.jpa.to.dto=Convert Entity to Dto

#prompts
action.prompt.convert.yaml=Convert each of these files `{0}` to the `.properties` format. Remain commentaries if there is any. Save result files in the original directory
action.prompt.convert.properties=Convert each of these files `{0}` to the `.yaml` format. Remain commentaries if there is any. Save result files in the original directory
action.prompt.convert.controller=Convert these classes `{0}` to the `openapi yaml files`. Remain commentaries if there is any. Save result to separate files and to the corresponding directory
action.prompt.convert.openapi=Convert these files `{0}` to the `Spring Controller files`. Remain commentaries if there is any. Save result to separate files and to the corresponding directory
action.prompt.convert.entity=Create new `DTO classes` from these `Jpa Entity classes` `{0}`. Remain commentaries if there is any. Save result files to the corresponding directory
action.prompt.convert.dto=Create new `Jpa Entity classes` from these `DTO classes` `{0}`. Remain commentaries if there is any. Save result files to the corresponding directory
