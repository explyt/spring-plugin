# Task Context

User request: add tests for all tools from `SpringBootApplicationMcpToolset`.

Goals:
- Identify every tool exposed by `SpringBootApplicationMcpToolset`.
- Find the existing test style and conventions for MCP-related code in this project.
- Determine project Kotlin version / relevant Kotlin setup to keep tests idiomatic.
- Prepare enough context for direct implementation if scope is compact.

Deliverables expected later:
- Tests covering every tool from `SpringBootApplicationMcpToolset`.
- Relevant test execution results.
